import React from "react";
import Hero from "../components/hero";

const Home: React.FC = () => {
  return (
    <div className="h-screen flex flex-col">
      <Hero />
    </div>
  );
};

export default Home;
